diff --git a/include/unixconf.h b/include/unixconf.h
index 65f698780..e733fa703 100644
--- a/include/unixconf.h
+++ b/include/unixconf.h
@@ -114,5 +114,5 @@
 /* #define TIMED_DELAY */ /* usleep() */
 #endif
-#if defined(MACOS) && !defined(TIMED_DELAY)
+#if defined(MACOS) && !defined(TIMED_DELAY) && !defined(NO_TIMED_DELAY)
 #define TIMED_DELAY
 #endif
diff --git a/include/wintty.h b/include/wintty.h
--- a/include/wintty.h
+++ b/include/wintty.h
@@ -5,6 +5,25 @@
 #ifndef WINTTY_H
 #define WINTTY_H
 
+/* NOMUX_CAPTURE: shadow frame buffer for direct screen capture without tmux */
+#ifndef NOMUX_CAPTURE
+#define NOMUX_CAPTURE
+#endif
+#ifdef NOMUX_CAPTURE
+typedef struct { char ch; unsigned char fg; unsigned char attr; unsigned char decgfx; } nomux_cell;
+extern nomux_cell nomux_buf[24][80];
+extern void nomux_clear_screen(void);
+extern void nomux_clear_to_eol(int row, int col);
+extern void nomux_capture_write_screen(void);
+extern void nomux_capture_write_input_boundary(void);
+extern int nomux_anim_id;
+extern void nomux_putch(int ch);
+extern void nomux_set_attr(int attr);
+extern void nomux_end_attr(void);
+extern void nomux_set_fg(int color);
+extern void nomux_end_fg(void);
+extern char *nomux_capture_screen(void);
+extern void nomux_get_cursor(int *cx, int *cy);
+#endif
+
 #ifndef WINDOW_STRUCTS
 #define WINDOW_STRUCTS
 
diff --git a/win/tty/termcap.c b/win/tty/termcap.c
--- a/win/tty/termcap.c
+++ b/win/tty/termcap.c
@@ -634,6 +634,169 @@
     return putchar(c);
 }
 
+#ifdef NOMUX_CAPTURE
+static const char *
+nomux_capture_path(void)
+{
+    static const char *path = (const char *) 0;
+    static int path_checked = 0;
+
+    if (!path_checked) {
+        path = getenv("NOMUX_SCREEN_FILE");
+        path_checked = 1;
+    }
+
+    return path;
+}
+
+static const char *
+nomux_anim_path(void)
+{
+    static const char *path = (const char *) 0;
+    static int path_checked = 0;
+
+    if (!path_checked) {
+        path = getenv("NOMUX_ANIM_FILE");
+        path_checked = 1;
+    }
+
+    return path;
+}
+
+static int nomux_seq = 0;
+int nomux_anim_id = 0;
+
+/* Append a delimited animation-frame record to the anim log (if configured).
+ * Format per frame:
+ *   ---ANIM_FRAME:id=N seq=S
+ *   <ANSI-encoded screen>
+ *   ---CURSOR:cx,cy
+ *   ---END_ANIM
+ * Bumps nomux_anim_id; does NOT touch the screen file or SEQ. */
+static void
+nomux_write_anim_log(void)
+{
+    const char *anim_path = nomux_anim_path();
+    if (!anim_path)
+        return;
+
+    FILE *af = fopen(anim_path, "a");
+    if (!af)
+        return;
+
+    char *scr = nomux_capture_screen();
+    int cx, cy;
+    nomux_get_cursor(&cx, &cy);
+    nomux_anim_id++;
+    fprintf(af, "---ANIM_FRAME:id=%d seq=%d\n%s\n---CURSOR:%d,%d\n---END_ANIM\n",
+            nomux_anim_id, nomux_seq, scr, cx, cy);
+    fclose(af);
+}
+
+/* Called for intermediate animation frames: zap beams, hurtle/knockback
+ * steps, thrown-object trajectories, etc.  Appends the frame to the anim
+ * log; does NOT bump SEQ.  SEQ only advances at real input boundaries so
+ * the recorder's "next input-wait" signal isn't triggered by animations. */
+void
+nomux_capture_write_screen(void)
+{
+    nomux_write_anim_log();
+}
+
+/* Write the shadow buffer to the screen file AND bump SEQ.  Called ONLY at
+ * real input-wait boundaries (tty_nhgetch, tty_nh_poskey) — the points
+ * where the game blocks waiting for the next keypress.  This is the frame
+ * the recorder considers authoritative for the step. */
+void
+nomux_capture_write_input_boundary(void)
+{
+    const char *nomux_path = nomux_capture_path();
+    if (!nomux_path)
+        return;
+
+    {
+        FILE *nf = fopen(nomux_path, "w");
+        if (!nf)
+            return;
+
+        char *scr = nomux_capture_screen();
+        int cx, cy;
+        nomux_get_cursor(&cx, &cy);
+        nomux_seq++;
+        fprintf(nf, "%s\n---CURSOR:%d,%d\n---SEQ:%d\n", scr, cx, cy, nomux_seq);
+        fclose(nf);
+    }
+}
+
+/* NOMUX shadow frame buffer implementation */
+nomux_cell nomux_buf[24][80];
+static unsigned char nomux_fg_cur = 7;
+static unsigned char nomux_attr_cur = 0;
+static unsigned char nomux_decgfx_cur = 0;
+
+void nomux_clear_screen(void) {
+    int r, c;
+    for (r = 0; r < 24; r++)
+        for (c = 0; c < 80; c++) {
+            nomux_buf[r][c].ch = ' ';
+            nomux_buf[r][c].fg = 7;
+            nomux_buf[r][c].attr = 0; nomux_buf[r][c].decgfx = 0;
+        }
+}
+
+void nomux_clear_to_eol(int row, int col) {
+    int c;
+    if (row < 0 || row >= 24) return;
+    for (c = (col < 0 ? 0 : col); c < 80; c++) {
+        nomux_buf[row][c].ch = ' ';
+        nomux_buf[row][c].fg = 7;
+        nomux_buf[row][c].attr = 0;
+    }
+}
+
+void nomux_putch(int ch) {
+    int row, col;
+    if (!ttyDisplay || ch < 32) return;
+    row = ttyDisplay->cury;
+    col = ttyDisplay->curx;
+    if (row >= 0 && row < 24 && col >= 0 && col < 80) {
+        nomux_buf[row][col].ch = (char) ch;
+        nomux_buf[row][col].fg = nomux_fg_cur;
+        nomux_buf[row][col].attr = nomux_attr_cur;
+        nomux_buf[row][col].decgfx = nomux_decgfx_cur;
+    }
+}
+
+void nomux_set_attr(int attr) {
+    switch (attr) {
+    case ATR_INVERSE: nomux_attr_cur |= 1; break;
+    case ATR_BOLD:    nomux_attr_cur |= 2; break;
+    case ATR_ULINE:   nomux_attr_cur |= 4; break;
+    }
+}
+void nomux_end_attr(void) { nomux_attr_cur = 0; }
+void nomux_set_fg(int color) {
+    /* Mirror C's wc2_darkgray remap: when that option is on,
+     * hilites[CLR_BLACK] is wired to BRIGHT_BLACK so the terminal
+     * receives [90m instead of [30m (plain black is invisible
+     * against a black background).  The shadow buffer must track
+     * the *visible* color, not the nominal index, or the capture
+     * will diverge from what the terminal (and JS) sees. */
+    if (color == CLR_BLACK && iflags.wc2_darkgray)
+        color = 8;  /* index 8 -> ANSI 90m (bright black / dark gray) */
+    nomux_fg_cur = (color < 0 || color >= 16) ? 7 : (unsigned char) color;
+}
+void nomux_end_fg(void) { nomux_fg_cur = 7; }
+
+static int nomux_sgr_fg(int fg) {
+    if (fg >= 0 && fg <= 7) return 30 + fg;
+    if (fg >= 8 && fg <= 15) return 90 + (fg - 8);
+    return 37;
+}
+
+static char nomux_out[24 * 256];
+char *nomux_capture_screen(void) {
+    char *p = nomux_out;
+    int row, col, end, cur_fg, cur_attr;
+    /* tmux capture-pane -e format:
+     * - Color start: \033[97m (just the code, no 0; prefix)
+     * - Color end (to default): \033[39m (fg reset only)
+     * - Attr start: \033[7m (inverse), \033[1m (bold)
+     * - Attr end: \033[27m (end inverse), \033[22m (end bold)
+     * - Full reset: \033[0m only at end-of-line or when both attr+color reset
+     * Incremental: only emit what changes between cells. */
+    for (row = 0; row < 24; row++) {
+        cur_fg = 7; cur_attr = 0;
+        end = 79;
+        while (end >= 0
+               && (nomux_buf[row][end].ch == ' ' || nomux_buf[row][end].ch == 0)
+               && nomux_buf[row][end].attr == 0
+               && (nomux_buf[row][end].fg == 7 || nomux_buf[row][end].fg == 0))
+            end--;
+        int in_dec = 0;
+        for (col = 0; col <= end; col++) {
+            nomux_cell *c = &nomux_buf[row][col];
+            char ch = c->ch ? c->ch : ' ';
+            int fg = c->fg ? c->fg : 7;
+            int at = c->attr;
+            /* Emit incremental SGR changes */
+            if (at != cur_attr) {
+                /* Handle attr changes individually */
+                if ((at & 1) && !(cur_attr & 1)) p += sprintf(p, "\033[7m");
+                if (!(at & 1) && (cur_attr & 1)) p += sprintf(p, "\033[27m");
+                if ((at & 2) && !(cur_attr & 2)) p += sprintf(p, "\033[1m");
+                if (!(at & 2) && (cur_attr & 2)) p += sprintf(p, "\033[22m");
+                if ((at & 4) && !(cur_attr & 4)) p += sprintf(p, "\033[4m");
+                if (!(at & 4) && (cur_attr & 4)) p += sprintf(p, "\033[24m");
+                cur_attr = at;
+            }
+            if (fg != cur_fg) {
+                if (fg == 7) {
+                    p += sprintf(p, "\033[39m"); /* reset fg to default */
+                } else {
+                    p += sprintf(p, "\033[%dm", nomux_sgr_fg(fg));
+                }
+                cur_fg = fg;
+            }
+            if (c->decgfx && !in_dec) { *p++ = '\016'; in_dec = 1; }
+            else if (!c->decgfx && in_dec) { *p++ = '\017'; in_dec = 0; }
+            *p++ = ch;
+        }
+        if (in_dec) { *p++ = '\017'; in_dec = 0; }
+        /* End-of-line: reset to defaults — match tmux format */
+        if (cur_attr && cur_fg != 7) {
+            p += sprintf(p, "\033[0m");  /* both attr+color: full reset */
+        } else if (cur_attr) {
+            p += sprintf(p, "\033[0m");  /* attr only: full reset */
+        } else if (cur_fg != 7) {
+            p += sprintf(p, "\033[39m"); /* color only: fg reset */
+            cur_fg = 7; cur_attr = 0;
+        }
+        if (row < 23) *p++ = '\n';
+    }
+    *p = '\0';
+    return nomux_out;
+}
+
+void nomux_get_cursor(int *cx, int *cy) {
+    *cx = ttyDisplay ? ttyDisplay->curx : 0;
+    *cy = ttyDisplay ? ttyDisplay->cury : 0;
+}
+#endif /* NOMUX_CAPTURE */
+
 void
 xputs(const char *s)
 {
@@ -649,6 +778,9 @@
 {
     if (CE) {
         xputs(CE);
+#ifdef NOMUX_CAPTURE
+        nomux_clear_to_eol(ttyDisplay->cury, ttyDisplay->curx);
+#endif
     } else { /* no-CE fix - free after Harold Rynes */
         int cx = ttyDisplay->curx + 1;
 
@@ -673,6 +805,9 @@
         xputs(CL);
         home();
         /* set remembered data to all spaces */
+#ifdef NOMUX_CAPTURE
+        nomux_clear_screen();
+#endif
         erase_tty_screen();
     }
 }
@@ -760,6 +895,9 @@
 void
 graph_on(void)
 {
+#ifdef NOMUX_CAPTURE
+    nomux_decgfx_cur = 1;
+#endif
     if (AS)
         xputs(AS);
 }
@@ -767,6 +905,9 @@
 void
 graph_off(void)
 {
+#ifdef NOMUX_CAPTURE
+    nomux_decgfx_cur = 0;
+#endif
     if (AE)
         xputs(AE);
 }
@@ -848,6 +989,9 @@
 {
     if (nh_CD) {
         xputs(nh_CD);
+#ifdef NOMUX_CAPTURE
+        { int r; nomux_clear_to_eol(ttyDisplay->cury, ttyDisplay->curx); for (r = ttyDisplay->cury + 1; r < 24; r++) nomux_clear_to_eol(r, 0); }
+#endif
     } else {
         int cy = ttyDisplay->cury + 1;
 
@@ -1439,6 +1583,9 @@
 term_start_attr(int attr)
 {
     if (attr) {
+#ifdef NOMUX_CAPTURE
+        nomux_set_attr(attr);
+#endif
         const char *astr = s_atr2str(attr);
 
         if (astr && *astr)
@@ -1450,6 +1597,9 @@
 term_end_attr(int attr)
 {
     if (attr) {
+#ifdef NOMUX_CAPTURE
+        nomux_end_attr();
+#endif
         const char *astr = e_atr2str(attr);
 
         if (astr && *astr)
@@ -1482,11 +1632,18 @@
 term_end_color(void)
 {
     xputs(nh_HE);
+#ifdef NOMUX_CAPTURE
+    nomux_end_fg();
+    nomux_end_attr();
+#endif
 }
 
 void
 term_start_color(int color)
 {
+#ifdef NOMUX_CAPTURE
+    nomux_set_fg(color);
+#endif
     if (color == NO_COLOR)
         xputs(nh_HE); /* inline term_end_color() */
     else if (color < CLR_MAX && hilites[color] && *hilites[color])
diff --git a/win/tty/wintty.c b/win/tty/wintty.c
--- a/win/tty/wintty.c
+++ b/win/tty/wintty.c
@@ -1280,6 +1280,9 @@
         term_start_color(item->color);
     (void) putchar(ch);
+#ifdef NOMUX_CAPTURE
+    nomux_putch(ch);
+#endif
     ttyDisplay->curx++;
     if (item->color != NO_COLOR)
         term_end_color();
@@ -1282,6 +1282,9 @@
     if (flags.standout)
         standoutbeg();
     xputs(prompt);
+#ifdef NOMUX_CAPTURE
+    { int save_cx = ttyDisplay->curx; const char *np; for (np = prompt; *np; np++) { nomux_putch(*np); ttyDisplay->curx++; } ttyDisplay->curx = save_cx; }
+#endif
     ttyDisplay->curx += strlen(prompt);
     if (flags.standout)
         standoutend();
@@ -1587,6 +1590,9 @@
                         if (n == 2 && curr->identifier.a_void != 0
                             && curr->selected) {
                             char c = (curr->count == -1L) ? '*' : '#';
+#ifdef NOMUX_CAPTURE
+                            { int svx = ttyDisplay->curx; ttyDisplay->curx--; nomux_putch(c); ttyDisplay->curx = svx; }
+#endif
 
                             /* all selected: '*' vs count selected: '#' */
                             (void) putchar(c);
@@ -1598,9 +1604,15 @@
                                and requires referencing the cursor location */
                             toggle_menu_attr(TRUE, gcolor, ATR_NONE);
                             (void) putchar(curr->glyphinfo.ttychar);
+#ifdef NOMUX_CAPTURE
+                            { int svx = ttyDisplay->curx; ttyDisplay->curx--; nomux_putch(curr->glyphinfo.ttychar); ttyDisplay->curx = svx; }
+#endif
                             toggle_menu_attr(FALSE, gcolor, ATR_NONE);
                         } else {
                             (void) putchar(*cp);
+#ifdef NOMUX_CAPTURE
+                            { int svx = ttyDisplay->curx; ttyDisplay->curx--; nomux_putch(*cp); ttyDisplay->curx = svx; }
+#endif
                         }
                     } /* for *cp */
                     if (n > attr_n && (color != NO_COLOR || attr != ATR_NONE))
@@ -1924,6 +1933,9 @@
             attr = cw->data[i][0] - 1;
             if (cw->offx) {
                 (void) putchar(' ');
+#ifdef NOMUX_CAPTURE
+                nomux_putch(' ');
+#endif
                 ++ttyDisplay->curx;
             }
             term_start_attr(attr);
@@ -1947,10 +1959,16 @@
                         end_glyphout();
                     } else {
                         (void) putchar(*cp);
+#ifdef NOMUX_CAPTURE
+                        { int svx = ttyDisplay->curx; ttyDisplay->curx--; nomux_putch(*cp); ttyDisplay->curx = svx; }
+#endif
                     }
                     linestart = FALSE;
                 } else {
                     (void) putchar(*cp);
+#ifdef NOMUX_CAPTURE
+                    { int svx = ttyDisplay->curx; ttyDisplay->curx--; nomux_putch(*cp); ttyDisplay->curx = svx; }
+#endif
                 }
             }
             term_end_attr(attr);
@@ -2316,6 +2334,9 @@
     case NHW_BASE:
         tty_curs(window, x, y);
         (void) putchar(ch);
+#ifdef NOMUX_CAPTURE
+        nomux_putch(ch);
+#endif
         ttyDisplay->curx++;
         cw->curx++;
         break;
@@ -2474,6 +2495,9 @@
         term_start_attr(attr);
         while (*str && (int) ttyDisplay->curx < (int) ttyDisplay->cols - 1) {
             (void) putchar(*str);
+#ifdef NOMUX_CAPTURE
+            nomux_putch(*str);
+#endif
             str++;
             ttyDisplay->curx++;
         }
@@ -2356,6 +2380,9 @@
             (void) putchar(*str);
+#ifdef NOMUX_CAPTURE
+            nomux_putch(*str);
+#endif
             str++;
             ttyDisplay->curx++;
         }
@@ -3929,6 +3959,10 @@
     console_g_putch(in_ch);
     nhUse(ch);
 #endif /* WIN32CON */
+#ifdef NOMUX_CAPTURE
+    /* Strip high bit for DEC graphics, matching putchar(ch ^ 0x80) */
+    nomux_putch((int)(unsigned char)((ch & 0x80) ? (ch ^ 0x80) : ch));
+#endif
     return;
 }
 
@@ -4200,6 +4228,9 @@
     print_vt_code1(AVTC_INLINE_SYNC);
     term_curs_set(1);
     (void) fflush(stdout);
+#ifdef NOMUX_CAPTURE
+    nomux_capture_write_input_boundary();
+#endif
     /* Note: if raw_print() and wait_synch() get called to report terminal
      * initialization problems, then wins[] and ttyDisplay might not be
      * available yet.  Such problems will probably be fatal before we get
@@ -4273,6 +4304,9 @@
     HUPSKIP_RESULT('\033');
 #if defined(WIN32CON)
     (void) fflush(stdout);
+#ifdef NOMUX_CAPTURE
+    nomux_capture_write_input_boundary();
+#endif
     /* Note: if raw_print() and wait_synch() get called to report terminal
      * initialization problems, then wins[] and ttyDisplay might not be
      * available yet.  Such problems will probably be fatal before we get
@@ -4962,6 +4996,9 @@
             n = i + x;
             if (n < ncols && *text) {
                 (void) putchar(*text);
+#ifdef NOMUX_CAPTURE
+                nomux_putch(*text);
+#endif
                 ttyDisplay->curx++;
                 cw->curx++;
                 cw->data[y][n - 1] = *text;
@@ -803,6 +803,10 @@
                 (void) putchar(c);
 #endif
+#ifdef NOMUX_CAPTURE
+                nomux_putch(c);
+                ttyDisplay->curx++;
+#endif
                 svp.plname[ct++] = c;
 #ifdef WIN32CON
                 ttyDisplay->curx++;
diff --git a/win/tty/topl.c b/win/tty/topl.c
--- a/win/tty/topl.c
+++ b/win/tty/topl.c
@@ -367,6 +367,9 @@
     if (cw->curx == 0)
         cl_end();
 #ifndef WIN32CON
     (void) putchar(c);
 #endif
+#ifdef NOMUX_CAPTURE
+    /* On non-WIN32CON, curx is incremented BEFORE putchar (see line 336
+       above), so curx is one ahead of where the character was actually
+       written.  Temporarily decrement to write to the correct shadow
+       buffer position — same pattern used by process_menu_window,
+       process_text_window, and other NOMUX hooks in wintty.c. */
+    { int svx = ttyDisplay->curx; ttyDisplay->curx--; nomux_putch(c); ttyDisplay->curx = svx; }
+#endif
 }
diff --git a/win/tty/termcap.c b/win/tty/termcap.c
--- a/win/tty/termcap.c
+++ b/win/tty/termcap.c
@@ -793,8 +793,26 @@ tty_delay_output(void)
 #if defined(MICRO)
     int i;
 #endif
-    if (iflags.debug_fuzzer)
-        return;
+    static int no_delay = -1;
+    const char *no_delay_env;
+
+    if (no_delay < 0) {
+        no_delay_env = getenv("NETHACK_NO_DELAY");
+        if (!no_delay_env || !*no_delay_env)
+            no_delay_env = getenv("WEBHACK_NO_DELAY");
+        no_delay = (no_delay_env && *no_delay_env && *no_delay_env != '0');
+    }
+    if (no_delay || iflags.debug_fuzzer) {
+        (void) fflush(stdout);
+#ifdef NOMUX_CAPTURE
+        /* Capture the current screen at this animation boundary.
+           This lets the session recorder see intermediate display
+           states (zap beams, thrown objects, explosions) that would
+           otherwise be invisible because the delay is suppressed.
+           nomux_capture_write_screen appends to the anim log and
+           bumps nomux_anim_id, which we include in the event marker
+           so the appendix entry can be cross-referenced. */
+        nomux_capture_write_screen();
+#endif
+        return;
+    }
 #ifdef TIMED_DELAY
     if (flags.nap) {
         (void) fflush(stdout);
